# AGENTS.md

本文件用于向 AI 编程助手说明本仓库的约定和工作流,避免新会话重复踩坑。修改本仓库前请先阅读。

## 项目简介

个人英语自学仓库,基于 VitePress 文档站(`pnpm dev` / `pnpm build`),核心理念是"以真实输出为核心":听 → 复述 → 发现问题 → 提炼表达 → 替换练习 → 再次输出 → 定期复盘。

主要练习材料:Family Album, U.S.A.(走遍美国),按集数(episode)和小节(act)进行脱稿复述。

目录结构:

- `docs/method/` 学习方法
- `docs/practice/` 训练记录(当前主要是 `family-album-usa/`)
- `docs/expressions/` 表达管理
- `docs/mistakes/` 错误记录
- `docs/grammar/` 语法知识(曾被误删,已恢复)
- `docs/pronunciation/` 发音知识:音标、音素、连读、重读、意群(曾被误删,已恢复)
- `docs/reviews/` 复盘模板
- `docs/archive/` 归档

## 硬性约定(Hard Rules)

1. **所有 commit 消息必须用英文写**,不要用中文。
2. **练习记录中不要提及录音文件**。音频不存放在仓库里,记录里不需要写音频文件名或位置(基本信息里只写"录音时长")。
3. **不要提交音频文件**(m4a、wav 等)。临时分析用的文件用完删除。
4. **代码块内每行不超过 80 字符**。代码块(```text 等)默认不自动换行,长行会导致横向滚动;普通段落、列表、引用会自动换行,不需要手动折行。检查方法:`awk 'BEGIN{infence=0} /^```/{infence=!infence} infence && length($0)>80 {print NR}' <file>`。
5. **大规模删除或重构前先和用户确认范围**。历史教训:`e2145f6` 一次重构删除了 grammar、pronunciation、tense 等知识目录,用户需要恢复,只能从旧提交里找回。删除知识类内容(语法、发音、时态等)务必谨慎。
6. 与用户用中文交流;练习记录、分析文档用中文写。

## 更新练习记录的 Checklist

新增或修改 `docs/practice/family-album-usa/` 下的记录后,必须同步以下入口,否则侧边栏/首页看不到:

- [ ] `docs/.vitepress/config.mts`:侧边栏 `'/practice/'` 分区手动加条目(此文件是手动维护的,不会自动生成)
- [ ] `docs/index.md`:首页 hero 的"最新练习"链接指向最新一条记录
- [ ] `docs/practice/family-album-usa/index.md`:练习记录索引列表

## 录音分析工作流

用户的口语练习录音存在 macOS"语音备忘录"里,终端无法直接访问(`Operation not permitted`,TCC 权限限制)。流程:

1. 请用户把 m4a 导出/复制到可访问的位置(如桌面或仓库目录),再读取。
2. `readFile` 直接读 m4a 可能失败(文件名含中文等),先用 `afconvert` 转成 WAV:`afconvert -f WAVE -d LEI16@16000 -c 1 in.m4a out.wav`,然后转写 WAV。
3. 音频指标分析用 `python3` + `audioop`(macOS 自带 Python 3.12 可用,无 numpy):50ms 帧 RMS,先看分布选阈值(本仓库录音噪底约 100–150,阈值 250–400 结果稳定),统计:总时长、有效发声时间、静音占比、超过 3 秒/5 秒的停顿次数、最长停顿。
4. 词数统计:`re.findall(r"[A-Za-z']+", text)`,语速 = 词数 / 时长。
5. **转写结果要谨慎对待**:Whisper 可能幻听(如把名字听成 `male member`、重复短句),需要和剧情原文对照;用 webSearch/webFetch 查 Family Album 对应 Act 的脚本作为基准。
6. 识别不稳定的人名/专有名词不直接算口语错误,标注为"存疑,需结合原音确认"。
7. 分析完删除临时 WAV 文件。

## 练习记录文件结构

文件命名:`episode-{ep}-act-{act}.md`(如 `episode-02-act-03.md`)。结构:

1. 基本信息:日期、材料、范围、录音时长、练习形式(不写音频文件)
2. 情节关键词(简洁的剧情线索)
3. 原始语音转写:原样保留识别结果,说明中注明"识别错误不算口语错误"
4. 音频数据:词数、整体语速、发声语速、静音占比、停顿统计
5. 与上一次录音相比:指标对比表格;新材料 vs 同一材料的进步要分开表述(区分"熟练度提升"和"能力迁移")
6. 已经出现的进步 / 当前主要问题
7. 语音识别需要谨慎判断的部分(表格:机器识别 → 推测的实际表达)
8. 本次暴露的表达问题:每条给"原表达 / 建议",按情节顺序编号
9. 修正后的复述(正确的完整版本)
10. 专有词降级表达(想不起原词时用什么简单说法)
11. 下一轮训练方法(具体、可量化:时长目标、停顿次数目标、固定开头/结尾句)
12. 后续复习 checklist(checkbox)

## 其他注意事项

- 每轮复述的目标指标会延续(如"超过 3 秒停顿 ≤ 3 次")。新记录要检查上一轮的"下一轮训练方法"和"后续复习"是否达成,并如实写入对比。
- 换新材料时,停顿、时长等指标回退不能直接判为退步,要说明材料差异。
- VitePress 构建验证:`pnpm build`(约 2 秒)。
